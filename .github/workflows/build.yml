name: Build

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-22.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          args: "-f markdown+rebase_relative_paths+gfm_auto_identifiers --toc -s -t gfm tickets/**/*.md -o README.md"